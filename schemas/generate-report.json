{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Generate Report",
  "description": "Output schema for adversarial GENERATE agent step",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["COMPLETE", "PARTIAL"]
    },
    "files": {
      "type": "array",
      "items": { "type": "string" }
    },
    "hardening": { "type": "string" },
    "issues_addressed": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["status", "files"]
}
